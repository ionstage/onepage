{
  "name": "onepage",
  "version": "0.1.0",
  "scripts": {
    "bundle": "browserify -r circuit -r js-base64 -i buffer -o js/bundle.js",
    "start": "browser-sync start --files='index.html, css/*.css, js/*.js' --server"
  },
  "dependencies": {
    "circuit": "^0.1.4",
    "js-base64": "^2.1.8"
  },
  "devDependencies": {
    "browser-sync": "^2.7.6",
    "browserify": "^10.2.4",
    "font-awesome-svg-png": "^1.1.5"
  },
  "private": true
}
